<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <!-- 권한 추가 시작 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" /> <!--카메라 사용 권한-->
    <!--공용 외부 저장소에 저장된 이미지 쓰기/읽기 권한-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera.autofocus" android:required="true"/>

    <!-- 권한 추가 끝 -->

    <application
        android:name="org.tensorflow.codelabs.objectdetection.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ObjectDetection"
        >

        <activity android:name="org.tensorflow.codelabs.objectdetection.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name="org.tensorflow.codelabs.objectdetection.SignInActivity"
            android:exported="true">
        </activity>

        <activity
            android:name="org.tensorflow.codelabs.objectdetection.SignUpActivity"
            android:exported="false" />

        <activity
            android:name="org.tensorflow.codelabs.objectdetection.MainActivity"
            android:exported="true"
            tools:ignore="MissingClass"> <!-- export 속성 추가 -->
        </activity>

        <activity
            android:name="org.tensorflow.codelabs.objectdetection.ActivityIngredient"
            android:exported="true" />

        <activity android:name="org.tensorflow.codelabs.objectdetection.SavedIngredientsActivity" />
        <activity android:name="org.tensorflow.codelabs.objectdetection.RecipeActivity" />
        <activity android:name="org.tensorflow.codelabs.objectdetection.RecipeDetailActivity" /> <!-- RecipeDetailActivity 추가 -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="org.tensorflow.codelabs.objectdetection.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" /><!-- directory 저장하는 xml 파일-->
        </provider>

    </application>

</manifest>
